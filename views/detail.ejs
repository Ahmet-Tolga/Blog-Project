<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="panel mt-4" style="width: 100%;height: 500px;box-shadow:3px 7px rgba(0, 10, 200)">
                <div class="row">
                    <div class="col-md-4">
                        <img src="data:image/<%= blog.img.contentType %>;base64,<%= blog.img.data.toString("base64") %>" alt="" width="400" height="400" >
                    </div>
                    <div class="col-md-4">
                        <h2><span class="text-muted">Title: </span><%= blog.title %></h2>
                        <p><h2><span class="text-muted">Description: </span></h2><%= blog.description %></p>
                        <h2><span class="text-muted">Created by: </span><%= user.username %></h2>
                        <h4><span class="text-muted">Created at: </span><%= blog.createdAt %></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>